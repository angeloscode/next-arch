'use client';

/**
 * ПРИМЕР: Sonner Toaster
 *
 * Подключи <Toaster /> один раз в app/layout.tsx
 */

import { Toaster } from 'sonner';

export function SonnerToaster() {
  return <Toaster richColors position="top-right" />;
}
